targetted_decisions = {
	become_waqil = {
		only_playable = yes
		is_high_prio = yes
		filter = religious_head
		ai_target_filter = religious_head
		ai_check_interval = 60
		only_independent = yes
		
		from_potential = {
			religion = shiite
			has_secret_religion = no
			prisoner = no
		}
		potential = {
			is_playable = yes
			religion = shiite
			has_landed_title = d_shiite
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			FROM = {
				piety = 50
				war = no
				relative_realm_size = {
					who = ROOT
					size >= 1
				}
				
			}
		}
		effect = {
			d_shiite = {
				holder_scope = {
					save_event_target_as = old_al_waqil
				}
				usurp_title = FROM
			}
			character_event = { id = twelver.2 }
			FROM = {
				character_event = { id = twelver.1 }
				piety = -25
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0.5
				trait = content
			}
		}
	}	
	embrace_qomi = { #i suppose this is supposed to be targeted
		only_playable = yes
		
		filter = self
		ai_target_filter = self
		ai_check_interval = 60
		
		potential = {
			has_dlc = "Sons of Abraham"
			is_playable = yes
			religion = shiite
			NOR = { 
				trait = qomi
				trait = najafi
			}
			is_adult = yes
			prisoner = no
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			piety = 50
			learning = 8
		}
		
		effect = {
			character_event = { id = MSI.00 }
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				trait = pious
			}
			modifier = {
				factor = 1.2
				trait = zealous
			}
			modifier = {
				factor = 1.2
				liege = { trait = qomi }
			}
			modifier = {
				factor = 0.5 # Slow it down
			}
		}
	}
	embrace_najafi = {
		only_playable = yes
		ai_check_interval = 60
		
		filter = self
		ai_target_filter = self
		
		potential = {	
			has_dlc = "Sons of Abraham"
			is_playable = yes
			religion = shiite
			NOR = { 
				trait = qomi
				trait = najafi
			}
			is_adult = yes
			prisoner = no
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		
		allow = {
			piety = 50
			learning = 8
		}
		
		effect = {
			character_event = { id = MSI.01 }
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				trait = zealous
			}
			modifier = {
				factor = 1.2
				trait = pious
			}
			modifier = {
				factor = 1.2
				liege = { trait = najafi }
			}
			
			modifier = {
				factor = 0.5 # Slow it down
			}
		}
	}
}