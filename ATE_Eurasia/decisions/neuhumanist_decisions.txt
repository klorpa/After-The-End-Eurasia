decisions = {
	lead_research = { #Philosophical wankery
		is_high_prio = yes
		
		ai_check_interval = 24
		
		potential = {
			NOT = { #immersion-breaking
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			OR = {
				is_ruler = yes
				controls_religion = yes
			}
			religion = neuhumanist
			age = 16
			prisoner = no
			NOT = { is_incapable = yes }
			NOT = { has_character_modifier = intense_writing }
			NOT = { has_character_flag = currently_working_on_a_book }
			NOT = { has_character_flag = wanting_to_quit }
			NOT = { has_character_modifier = philosophic_work_done}
		}
		allow = {
			war = no
			NOT = { has_character_modifier = philosophic_work_done}
		}
		effect = {
			add_character_modifier = { name = intense_writing duration = -1 }
			hidden_tooltip = {
				character_event = {
					id = neuhumanist.2
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = { factor = 0.3 }
			modifier = { factor = 2	trait = zealous }
			modifier = { factor = 2	trait = diligent }
			modifier = { factor = 0.3 trait = slothful }
			modifier = { factor = 0.3 trait = cynical }
		}
	}
	
	end_research = {
		is_high_prio = yes
		only_playable = yes
		ai_check_interval = 24
		
		potential = {
			NOT = { #immersion-breaking
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			OR = {
				is_ruler = yes
				controls_religion = yes
			}
			religion = neuhumanist
			age = 16
			OR = {
			has_character_modifier = intense_writing
			has_character_flag = wanting_to_quit
			}
		}
		effect = {
			remove_character_modifier = intense_writing
			piety = -50
			prestige = -50
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = { factor = 0 }
		}
	}
	nh_salon_visit = {
		is_high_prio = yes
		only_playable = yes
		ai_check_interval = 120
		
		potential = {
			religion = neuhumanist
			age = 16
			prisoner = no
			in_seclusion = no
			NOT = { has_character_modifier = salon_done }
			NOT = { is_incapable = yes }
			NOT = { trait = on_pilgrimage }
			NOT = { trait = legendary }
			NOT = { #immersion-breaking
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			piety = 100
			wealth = 50
		}
		effect = {
			add_trait = on_pilgrimage
			wealth = -50
			hidden_tooltip = {
				character_event = {
					id = nh_seance.1
				}
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 8 }
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			modifier = { factor = 0.001 }
			modifier = { factor = 10 num_of_count_titles = 1 }
			modifier = { factor = 2	trait = zealous }
			modifier = { factor = 2	trait = brave }
			modifier = { factor = 2	trait = ambitious }
			modifier = { factor = 0 trait = cynical }
			modifier = { factor = 0 trait = craven }
			modifier = { factor = 0 trait = content }
			modifier = { factor = 0 NOT = { wealth = 250 } }
		}
	}
}