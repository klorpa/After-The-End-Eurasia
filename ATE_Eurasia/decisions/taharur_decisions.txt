decisions = {
	lead_taharur = {
		only_playable = yes
		is_high_prio = yes
		filter = independent_rulers
		ai_check_interval = 12
		only_independent = yes
		
		potential = {
			is_playable = yes
			religion = taharur
			higher_real_tier_than = DUKE
			d_taharur = { has_holder = no }
			
		}
		allow = {
			war = no
			learning = 16
			martial = 16
			is_dishonorable = no
			calc_true_if = {
				amount >= 2
				trait = brave
				trait = honorable
				trait = honest
				trait = just
				trait = chaste
				trait = temperate
				trait = charitable
				trait = diligent
				trait = patient
				trait = kind
				trait = humble
			}
			calc_true_if = {
				amount >= 2
				is_sufi = yes
				trait = hafiz
				trait = faqih
				trait = scholar
				trait = sufi_zahid
				trait = mystic
			}
			piety = 2000
			prestige = 2000
		}
		effect = {
			d_taharur = {
				gain_title = ROOT
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 2
				waqil = {
					relative_realm_size = {
						who = ROOT
						size <= 0.90
					}
				}
			}
		}
	}
}
