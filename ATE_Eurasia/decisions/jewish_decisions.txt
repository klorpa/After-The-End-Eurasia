decisions = {
	switch_haredi = {
		ai = no
		
		potential = {
			religion = haredi
			is_playable = yes
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			NOT = { has_character_flag = changedharedibranch }
		}
		
		allow = {
			religion = haredi
			is_playable = yes
			piety = 500
		}
		
		effect = {
			hidden_tooltip = {
				narrative_event = { id = jewish.1 }				
			}
			piety = -250
			add_character_modifier = { name = haredi_outrage years = 10 }
			set_character_flag = changedharedibranch
		}
		
		revoke_allowed = {
			always = no
		}
	}
	restore_ivri_rabbi = {
		only_independent = yes
		is_high_prio = yes
		ai_check_interval = 36
		
		potential = {
			is_playable = yes
			independent = yes
			has_dlc = "Sons of Abraham"
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
			religion = ivri
			
			controls_religion = no
			
			c_west_jerusalem = {
				holder_scope = {
					OR = {
						character = ROOT
						is_liege_or_above = ROOT
					}
					AND = {
						ROOT = { religion = ivri }
						d_ivri = {
							holder_scope = {
								NOT = { character = PREVPREV }
								NOT = { is_vassal_or_below = PREVPREV }
							}
						}
					}
				}
			}
		}
		
		effect = {		
			if = {
				limit = {
					religion = ivri
				}
				d_ivri = {
					holder_scope = {
						c_west_jerusalem = {
							grant_title = PREV
							any_de_jure_vassal_title = {
								limit = {
									holder_scope = { 
										OR = {
											character = ROOT
											is_liege_or_above = ROOT
										}
									}
								}
								grant_title = PREVPREV
							}
						}
					}
				}
			}
			if = {
				limit = {
					religion = ivri
					NOT = { has_character_flag = restored_chief_rabbi }
				}
				d_ivri = {
					holder_scope = {
						opinion = {
							who = ROOT
							modifier = opinion_restored_chief_rabbi
						}
					}
				}
				piety = 1000
				set_character_flag = restored_chief_rabbi
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					any_demesne_province = {
						NOT = { province_id = 1958 }
					}
				}
			}
			modifier = {
				factor = 5
				trait = zealous
			}
			modifier = {
				factor = 0
				c_west_jerusalem = {
					holder_scope = {
						OR = {
							ai = no
							any_liege = { ai = no } # AI will never take West Jerusalem province away from a player
						}
					}
				}
			}
		}
	}
	celebrate_tish = {
		only_playable = yes
		is_high_prio = yes
		ai_check_interval = 3
		potential = {
			is_playable = yes
			is_adult = yes
			prisoner = no
			religion = haredi
			trait = haredi_chassid
			NOT = {	has_character_flag = tish_in_progress }
			NOT = {
				has_alternate_start_parameter = { key = religion_names value = random }
			}
		}
		allow = {
			war = no
			custom_tooltip = {
				text = UNOCCUPIED_DEMESNE_TITLE
				hidden_tooltip = {
					any_demesne_title = {
						NOT = { higher_tier_than = count }
						is_occupied = no
					}
				}
			}
			wealth = 100
			prisoner = no
			NOT = { is_inaccessible_trigger = yes }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			wealth = -100
			piety = 50
			set_character_flag = tish_in_progress
			set_character_flag = recent_minor_decision
			set_character_flag = do_not_disturb
			custom_tooltip = {
				text = tish_decision
				hidden_tooltip = { 
					character_event = { id = jewish.10 } 
				}
			}
			if = {
				limit = {
					has_dlc = "Reapers"
				}
				custom_tooltip = {
					text = capital_prospers_custom_tooltip
					hidden_tooltip = {
						capital_scope = {
							change_variable = { which = prosperity_value value = 8 }
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0
				NOT = {
					wealth = 200
				}
			}
			modifier = {
				factor = 0
				trait = cynical
			}	
			modifier = {
				factor = 0
				has_character_flag = recent_minor_decision
				NOT = {
					had_character_flag = {
						flag = recent_minor_decision
						days = 730
					}
				}
			}
		}
	}
	
	# attend_yeshiva ={
		# only_playable= yes
		# is_high_prio = yes
	
		# filter = self
		# ai_target_filter = self
		# ai_check_interval = 24
		
		# potential = {			
			# is_playable = yes
			# is_adult = yes
			# religion_group = jewish_group
			# OR = {
				# has_focus = focus_theology
				# trait = haredi_yashvai
			# }
			# NOR = { 
				# trait = incapable
				# trait 
				# is_inaccessible_trigger = yes
				# AND = {
					# OR= {
						# religion = haredi
						# religion = ivri
					# }
					# is_female = yes
				# }
			# }
			# war = no
			# prisoner = no
		# }
		
		# allow = {
			# NOR = {
				# is_ill = yes
				# has_character_flag = talmud_studytimer
			# }
			# piety = 100
			# wealth = 50
		# }
		# effect = {
			# custom_tooltip = { text = TXT_talmud_study		}
			# set_character_flag = talmud_studytimer
			# wealth = -50
			# hidden_tooltip = {
				# narrative_event = { id = jewish.150 }				
			# }
		# }
		# revoke_allowed = {
			# always = no
		# }
		# ai_will_do = {
			# factor =.05
			# modifier = {
				# factor = 5
				# trait = erudite
			# }
		# }	
	# }
	
	form_empire_pale = {
		is_high_prio = yes
		only_playable = yes
		ai = no

		potential = {
			religion_group = jewish_group
			culture = yiddish
			any_demesne_title = {
				OR = {
					tier = KING
					tier = EMPEROR
				}
				region = custom_pale
			}
			NOR = {
				has_landed_title = e_ottoman_emprie
				has_landed_title = e_europa
			}
			e_pale_settlement = {
				is_titular = yes
			}
			e_pale_settlement = {
				has_holder = no
			}
			is_alternate_start = no
		}

		allow = {
			is_adult = yes
			independent = yes
			war = no
			prestige = 2000

			custom_tooltip = {
				text = completely_control_pale_TT

				completely_controls_region = custom_pale
			}
		}

		effect = {
			hidden_tooltip = {
				primary_title = {
					e_pale_settlement = {
						copy_title_laws = PREV
						copy_title_history = PREV
					}
				}
			}

			custom_tooltip = {
				text = create_pale_tt
				grant_title_no_opinion = e_pale_settlement 
			}
			#this stuff isn't really necessary

			hidden_tooltip = {

				any_demesne_title = {
					limit = {
						tier = EMPEROR
						NOT = { title = e_pale_settlement }
					}
					destroy_landed_title = THIS
				}

				any_landed_title = {
					limit = {
						tier = KING
						region = custom_pale
					}
					de_jure_liege = e_pale_settlement
				}
				if = {
					limit = {
						completely_controls = k_poland
					}
					k_poland = {
						de_jure_liege = e_pale_settlement
					}
				}

				ROOT = {
					narrative_event = { id = jewish.22 }
				}
				any_playable_ruler = {
					narrative_event = { id = jewish.22 }
				}

			}
		}
	}

	
}

targeted_decisions = {						
	bar_mitzvah = {
		only_playable = yes
		is_high_prio = yes
		
		filter = home_court
		ai_target_filter = home_court
		ai_check_interval = 12
	
		from_potential = {
			is_playable = yes
			religion_group = jewish_group
			prisoner = no
			NOR = { 
				trait = incapable
				is_inaccessible_trigger = yes
			}
			war = no
		}
		
		potential = {
			is_alive = yes
			religion_group = jewish_group
			NOR = {
				has_character_flag = bar_mitzvahed
				AND = {
					OR= {
						religion = haredi
						religion = ivri
					}
					is_female = yes
				}
				
			}
			is_child_of = FROM
		}
		allow = {
			age = 12
			NOR = {
				is_landed = yes
				prisoner = yes
				is_adult = yes
				custom_tooltip = {
					text = bastard_eunuch_celibate_or_incapable_tt
					OR = {
						trait = bastard
						trait = eunuch
						trait = celibate
						trait = incapable
					}
				}
				is_ascetic_trigger = yes
			}
			is_inaccessible_trigger = no
			host = { character = FROM }
		}
		effect = {
			custom_tooltip = {
				text = bar_mitzvah_custom_tooltip
				hidden_tooltip = {
					set_character_flag = bar_mitzvahed
					save_event_target_as = bar_mitzvah_target
					FROM = { character_event = { id = jewish.101 } }
				}
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 1
			
			modifier = {
				factor = 0.1
				always = yes
			}
		}
	}
	
	
	interfaith_dialogue = {
		only_playable = yes
		filter = realm
		ai_target_filter = realm
		from_potential = {
			#Conditions on the decision taker for the decision to appear. Not taken into account when filter = self, use potential instead.
			religion = haskalah
			NOR = {
				has_character_flag = dialoguing
				has_character_modifier = recently_dialogued
			}
			NOT ={
				ROOT = {
					trigger_switch ={
						#### reminder to update as more religion groups are added
						on_trigger = religion_group
						christianity			= { FROM = { trait = sympathy_christendom}}
						vernacular	 			= { FROM = { trait = sympathy_vernacular } }
						#jewish_group 			= { FROM = { trait = sympathy_judaism }}
						pagan_group 			= { FROM = { trait = sympathy_pagans }}
						muslim 					= { FROM = { trait = sympathy_islam }}
						old_world_cultist_group	= { FROM = { trait = sympathy_old_world_cults }}
						islamosyncretic			= { FROM = { trait = sympathy_islamosyncretic }}
						scholastic				= { FROM = { trait = sympathy_scholastic }}
					}
				}	
			}
		}
		potential = {
			#Conditions on the targeted character for the decision to appear
			NOT = {religion_group = jewish_group}
			NOT = {has_trait = sympathy_judaism}
		}
		allow = {
			#Conditions for the decision to be enabled
			ROOT = {
				NAND = {
					trait = zealous
					has_character_modifier = expelled_jewish
				}
			}
			FROM = {
				piety = 50
			}
		}	
		effect = {
			#Commands executed when taking the decision
			FROM = {
				piety = -50
				set_character_flag = dialoguing
				save_event_target_as = dialogue_jew
			}
			ROOT = {
				character_event ={ id = jewish.31 days = 3}
				save_event_target_as = dialogue_gentile
			}
		}
		revoke_allowed = {
			always = no #Not used in vanilla
		}
		ai_will_do = {
			#Factors for an AI character to take the decision (1 = 100% chance every month - for each allowed target!)
			factor = 0 #needs to add more later, now at 0 so it won't bug shit
		}
	}
}